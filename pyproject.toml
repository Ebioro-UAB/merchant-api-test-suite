[project]
name = "ebioro-merchant-api-test-suite"
version = "1.1.0"
description = "API client libraries and test suite for the Ebioro Merchant API"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.1",
    "requests>=2.32.4",
]
